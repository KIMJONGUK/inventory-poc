---
name: 유지보수 작업
about: 작업 계약 6개 항목
title: "[유지보수] "
labels: maintenance
---

<!--
6개 항목이 비면 시작하지 않는다 — 계약이 없으면 완료를 판정할 수 없다 (docs/harness/ssot.md §4).
통째로 덮어써서 붙여넣어도 된다. 모르는 항목은 비워 두면 에이전트가 댓글로 초안을 내고 사람이 확정한다.
-->

## 1. 배경

<!-- 왜 필요한가. 지금 어떻게 동작하고 무엇이 불편한가. 구체적 숫자(재현 입력)는 여기에만. -->

관련 요구사항:
<!-- F1~F11 또는 DoD 중 어느 것인가 (docs/harness/ssot.md §1). 어디에도 없으면 새 요구사항이므로 먼저 사람이 정한다. -->

재현:

## 2. 변경할 내용

<!-- 어디를 고치나. 화면 이름 · 클릭 경로 · 아는 파일 경로. -->

## 3. 종료 조건

<!-- 관계식으로. 그 순간의 관찰값(`파손이 12 로 나온다`)은 쓰지 않는다.
     네 줄 중 지울 줄은 없다 — 해당이 없으면 `해당 없음 · 이유` 를 적는다. -->

- 정상 ·
- 경계 ·
- 거부 ·
- 불변 ·

## 4. 테스트 코드 기능명

<!-- 3번 네 줄을 판정할 테스트. 같은 라벨 · 같은 순서로 1:1. 없으면 새로 만들 이름을 적는다.
     개수가 3번과 어긋나면 그 자체가 모호다 (docs/harness/loop.md 부록 · 관문 1:1).
     판정은 화면이 아니라 npm run verify 가 한다. -->

- 정상 · `tests/____.test.ts` > 
- 경계 · `tests/____.test.ts` > 
- 거부 · `tests/____.test.ts` > 
- 불변 · `tests/____.test.ts` > 

## 5. 건드리면 안 되는 것

<!-- 변경 경계. 아래는 기본값 — 더할 것이 있으면 적는다. -->

- 보호 영역 전부 (`.github/workflows/verify.yml` 의 `PROTECTED` 목록이 원본)
- 기존 테스트의 기대값 (`docs/harness/verification.md` §3)
- `prisma/dev.db`

## 6. 구현 루프 최대 횟수

<!-- npm run verify 가 몇 번 실패하면 멈추고 사람에게 넘기나. 비우면 ssot.md §7 의 전역 기본값. -->

3회

## 참고 (선택)

<!-- 스크린샷 · 관련 이슈 · 급한 정도 -->

## 출처 (에이전트가 연 이슈만)

<!-- 대화로 요청받아 에이전트가 계약 초안을 만든 경우에만 채운다.
     이 절이 있는 이슈는 `contract-draft` 라벨이 붙어 있고, 라벨이 붙어 있는 한 루프는 열리지 않는다.
     사람이 라벨을 떼고 `계약 확정` 댓글을 달아야 시작한다 (docs/harness/loop.md §1). -->

요청 원문:

추측으로 채운 항목:
